function sayHi() {
  var userName = "John";
}
console.log("Hi, " + userName);
sayHi();


// Export
// Export Default 

// In one file we can have multiple exports, but only one default export.

// whenever you import default export, you can give any name to it.

export let months = ['Jan', 'Feb', 'Mar', 'Apr', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];


